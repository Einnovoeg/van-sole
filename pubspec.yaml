name: van_sole
description: "Van Solè is a cross-platform spacefaring action RPG built with Flutter."
publish_to: 'none'
version: 0.1.1+2

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
